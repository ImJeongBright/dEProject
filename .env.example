# Seoul Open Data API Key
# 신청: http://data.seoul.go.kr/
SEOUL_DATA_API_KEY=sample_key_12345

# PostgreSQL Warehouse Connection
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=bike_warehouse
POSTGRES_HOST=postgres-warehouse
POSTGRES_PORT=5432

# Airflow Meta Database (If needed explicitly, usually handled in docker-compose)
# Airflow Meta Database (If needed explicitly, usually handled in docker-compose)
AIRFLOW_UID=50000

# Slack Alert Integration
# https://api.slack.com/messaging/webhooks 에서 Webhook URL 생성 후 입력
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR_WORKSPACE/YOUR_CHANNEL/YOUR_KEY

# KMA Weather API Key
# 신청: https://www.data.go.kr/
WEATHER_API_KEY=sample_weather_key
